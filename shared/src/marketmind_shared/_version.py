# Schema version is bumped whenever the strategy spec shape changes
# in a way that's not backward-compatible. Phase 0 starts at 0.1.0
# (placeholder); Phase 1 will define the real strategy spec at 1.0.0.
SCHEMA_VERSION = "0.1.0"
