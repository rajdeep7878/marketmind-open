# C.1.3 cassette directory — OandaAdapter VCR cassettes.
# Cassettes here are YAML files capturing deterministic Oanda HTTP
# request/response pairs. Tests replay from cassettes; no network
# call ever leaves the test process.
